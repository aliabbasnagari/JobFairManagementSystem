@model JobFairManagementSystem.Data.ApplicationUser
<ul class="nav nav-pills flex-column mb-auto">
    <li class="nav-item">
        <a asp-controller="Company" asp-action="Home" class="nav-link @(ViewBag.active == 0 ? "active": "link-body-emphasis")">
            <i class="bi bi-house me-2"></i>Home
        </a>
    </li>
    <li>
        <a asp-controller="Company" asp-action="Home" class="nav-link @(ViewBag.active == 1 ? "active": "link-body-emphasis")">
            <i class="bi bi-speedometer2 me-2"></i>
            Dashboard
        </a>
    </li>
    <li>
        <a asp-controller="Company" asp-action="Update" asp-route-id="@Model.Id" class="nav-link @(ViewBag.active == 2 ? "active": "link-body-emphasis")">
            <i class="bi bi-building me-2"></i>Update Profile
        </a>
    </li>
    <li>
        <a asp-controller="Company" asp-action="CreateSchedule" asp-route-id="@Model.Id" class="nav-link @(ViewBag.active == 3 ? "active": "link-body-emphasis")">
            <i class="bi bi-columns-gap me-2"></i>Create Interview Schedule
        </a>
    </li>
    <li>
        <a href="#" class="nav-link @(ViewBag.active == 4 ? "active": "link-body-emphasis")">
            <i class="bi bi-send me-2"></i>Open House Invitations
        </a>
    </li>
    <li>
        <a asp-controller="Candidate" asp-action="ShowNotifications" class="nav-link @(ViewBag.active == 5 ? "active" : "link-body-emphasis")">
            <i class="bi bi-bell me-2"></i>Notifications
            <span class="badge text-bg-danger rounded-pill ms-2">@Model.Notifications.FindAll(n => !n.IsRead).Count</span>
        </a>
    </li>
</ul>